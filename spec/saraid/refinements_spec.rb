RSpec.describe Saraid::Refinements do
  it "has a version number" do
    expect(Saraid::Refinements::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
